import React from 'react';
import {
  HeroImage,
  HeroText,
  HeroTitle,
  HeroTextButton,
  HeroDescription,
} from './styles';

export const Announcement = () => {
  return (
    <HeroImage>
      <HeroText>
      </HeroText>
    </HeroImage>

  );
};
